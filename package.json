{
  "name": "nexusgate-sub",
  "version": "0.6.10",
  "private": true,
  "description": "NexusGate relay control plane with isolated SubVault subscription service",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check agent/agent.js && node --check public/app.js && node --check subvault/static/app.js && node --check subvault/static/login.js && node --check scripts/reset-password.js && node --check scripts/agent-install-verify.js && bash -n scripts/*.sh"
  },
  "license": "MIT"
}
